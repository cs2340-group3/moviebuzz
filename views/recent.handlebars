<ul class="movie_list">
  {{#each movies}}
  <li>{{this.title}}
    {{#if dvd}}
      (Released on: {{this.release_dates.dvd}})
    {{else}}
      (Released on: {{this.release_dates.theater}})
    {{/if}}
  </li>
  {{else}}
    <p>No result</p>
  {{/each}}
</ul>
